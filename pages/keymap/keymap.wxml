<!--pages/keymap/keymap.wxml-->
<view>
  <scroll-view>

    <view wx:for="{{keymapList}}" wx:key="{{item}}" class="card">
      <label class="keymap-label-style">{{item.option}}</label>
      <view>
       {{item.keymap}}
      </view>
    </view>

  </scroll-view>
</view>

