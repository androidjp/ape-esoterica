<!--pages/cmd/cmd.wxml-->
<view>
  <scroll-view>

    <view wx:for="{{cmdList}}" wx:key="{{item}}" class="card">
      <label class="common-label-style">{{item.option}}</label>
      <view>
        {{item.cmd}}
      </view>
    </view>

  </scroll-view>
</view>